#include "main.h"

/**
 * printAlpha - prints the alphabet in lowercase
 * Description: Check print alpha
 * Return: Void
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);
	_putchar('\n');
}
